<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a.univ_edt_ade">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EdtDisplay"
            android:label="EdtDisplay"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestNestedScrollView" />
        <activity android:name=".TestGridView" />
        <activity android:name=".ArboSelect" />
        <activity
            android:name=".TestNavDrawer"
            android:label="@string/title_activity_test_nav_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TestCustomLayout" />
        <activity android:name=".TestTouchEvent"></activity>
    </application>

</manifest>